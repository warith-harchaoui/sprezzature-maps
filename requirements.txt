# Runtime dependencies for local installs. pyproject.toml stays the single
# source of truth for version constraints — this just selects which of its
# extras to install, so there is nothing here to fall out of sync when a
# dependency's version bound changes.
#
# cli — the click-based CLI twin of the always-installed argparse `make-map`
# api — the FastAPI HTTP surface (also serves the GUI gallery page)
# mcp — the FastAPI-MCP surface, mounts the same app as MCP tools
-e .[cli,api,mcp]
